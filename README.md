# 慢病用药小管家（Med Minder）

面向慢病患者的“用药计划 + 到点提醒 + 服药打卡 + 续方提醒”网页应用。

## 当前状态

- 匿名试用：已预留信息架构与页面骨架
- iCal：已提供示例订阅接口 `GET /api/ical`
- AI：暂不接入（后续在闭环稳定后再加）

## 本地启动（需要 Node + npm/pnpm/yarn 任选其一）

```bash
cd med-minder
npm install
npm run dev
```

## 文档

- `docs/DEV_DOC.md`：产品闭环、页面、数据模型、里程碑、部署方案

